dist/
node_modules/
# We pin the editor version in package.json (kasimov#<commit>), not in the lock —
# the lock dragged in 2600 lines of transitive dev deps for one meaningful line.
package-lock.json
