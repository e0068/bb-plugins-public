// Слой 1 — id, под которым bb держит этот плагин. Его получают шаги
// (`StepPorts.ownPluginId`): шаг обновления плагинов никогда не обновляет на
// месте тот плагин, что ведёт прогон, — обновление гасит его API-хэндл и
// оборвало бы цепочку (server/automation-runner.ts).
//
// Расхождение с именем пакета не «ломает шаг», а даёт `update` вместо
// `update-self`, то есть обрыв прямо внутри шага, — поэтому оно прибито
// тестом к package.json.
export const OWN_PLUGIN_ID = "flow";
