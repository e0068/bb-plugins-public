// Предел содержимого скрипта-шага: скрипт хранится в настройках flow, а не файлом.
// Значение в lib, а не в контракте: его читает и фронт, в бандл которого контракт не попадает.
export const MAX_SCRIPT_CHARS = 200_000;
